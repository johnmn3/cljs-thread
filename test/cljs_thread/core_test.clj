;; Test file for cljs-thread.core (Clojure tests)
(ns cljs-thread.core-test
  (:require [clojure.test :refer :all]))

(deftest dummy-clj-test
  (testing "A dummy Clojure test in core_test.clj"
    (is (= 1 1) "This should pass")))
