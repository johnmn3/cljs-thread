shadow$provide.module$node_modules$d3_hierarchy$src$hierarchy$path = function(global, require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  const $$default = function(end) {
    var start = this;
    var ancestor = start;
    var b = end;
    if (ancestor !== b) {
      var aNodes = ancestor.ancestors(), bNodes = b.ancestors(), c = null;
      ancestor = aNodes.pop();
      for (b = bNodes.pop(); ancestor === b;) {
        c = ancestor, ancestor = aNodes.pop(), b = bNodes.pop();
      }
      ancestor = c;
    }
    for (b = [start]; start !== ancestor;) {
      start = start.parent, b.push(start);
    }
    for (start = b.length; end !== ancestor;) {
      b.splice(start, 0, end), end = end.parent;
    }
    return b;
  };
};

//# sourceMappingURL=module$node_modules$d3_hierarchy$src$hierarchy$path.js.map
