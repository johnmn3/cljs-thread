shadow$provide.module$node_modules$d3_time$src$month = function(global, require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, timeMonth:{enumerable:!0, get:function() {
    return timeMonth;
  }}, timeMonths:{enumerable:!0, get:function() {
    return timeMonths;
  }}, utcMonth:{enumerable:!0, get:function() {
    return utcMonth;
  }}, utcMonths:{enumerable:!0, get:function() {
    return utcMonths;
  }}});
  global = require("module$node_modules$d3_time$src$interval");
  const timeMonth = (0,global.timeInterval)(date => {
    date.setDate(1);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setMonth(date.getMonth() + step);
  }, (start, end) => end.getMonth() - start.getMonth() + 12 * (end.getFullYear() - start.getFullYear()), date => date.getMonth()), timeMonths = timeMonth.range, utcMonth = (0,global.timeInterval)(date => {
    date.setUTCDate(1);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCMonth(date.getUTCMonth() + step);
  }, (start, end) => end.getUTCMonth() - start.getUTCMonth() + 12 * (end.getUTCFullYear() - start.getUTCFullYear()), date => date.getUTCMonth()), utcMonths = utcMonth.range;
};

//# sourceMappingURL=module$node_modules$d3_time$src$month.js.map
