shadow$provide.module$node_modules$d3_scale$src$time = function(global, require, module, exports) {
  function date(t) {
    return new Date(t);
  }
  function number(t) {
    return t instanceof Date ? +t : +new Date(+t);
  }
  function calendar(ticks, tickInterval, year, month, week, day, hour, minute, second, format) {
    function tickFormat(date) {
      return (second(date) < date ? formatMillisecond : minute(date) < date ? formatSecond : hour(date) < date ? formatMinute : day(date) < date ? formatHour : month(date) < date ? week(date) < date ? formatDay : formatWeek : year(date) < date ? formatMonth : formatYear)(date);
    }
    var scale = (0,default$$require$_DOT__SLASH_continuous_DOT_js.default)(), invert = scale.invert, domain = scale.domain, formatMillisecond = format(".%L"), formatSecond = format(":%S"), formatMinute = format("%I:%M"), formatHour = format("%I %p"), formatDay = format("%a %d"), formatWeek = format("%b %d"), formatMonth = format("%B"), formatYear = format("%Y");
    scale.invert = function(y) {
      return new Date(invert(y));
    };
    scale.domain = function(_) {
      return arguments.length ? domain(Array.from(_, number)) : domain().map(date);
    };
    scale.ticks = function(interval) {
      var d = domain();
      return ticks(d[0], d[d.length - 1], null == interval ? 10 : interval);
    };
    scale.tickFormat = function(count, specifier) {
      return null == specifier ? tickFormat : format(specifier);
    };
    scale.nice = function(interval) {
      var d = domain();
      interval && "function" === typeof interval.range || (interval = tickInterval(d[0], d[d.length - 1], null == interval ? 10 : interval));
      return interval ? domain((0,default$$require$_DOT__SLASH_nice_DOT_js.default)(d, interval)) : scale;
    };
    scale.copy = function() {
      return (0,require$_DOT__SLASH_continuous_DOT_js.copy)(scale, calendar(ticks, tickInterval, year, month, week, day, hour, minute, second, format));
    };
    return scale;
  }
  function time() {
    return require$_DOT__SLASH_init_DOT_js.initRange.apply(calendar(require$d3_time.timeTicks, require$d3_time.timeTickInterval, require$d3_time.timeYear, require$d3_time.timeMonth, require$d3_time.timeWeek, require$d3_time.timeDay, require$d3_time.timeHour, require$d3_time.timeMinute, require$d3_time.timeSecond, require$d3_time_format.timeFormat).domain([new Date(2000, 0, 1), new Date(2000, 0, 2)]), arguments);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, calendar:{enumerable:!0, get:function() {
    return calendar;
  }}, default:{enumerable:!0, get:function() {
    return time;
  }}});
  var require$d3_time = require("module$node_modules$d3_time$src$index"), require$d3_time_format = require("module$node_modules$d3_time_format$src$index"), require$_DOT__SLASH_continuous_DOT_js = require("module$node_modules$d3_scale$src$continuous"), require$_DOT__SLASH_init_DOT_js = require("module$node_modules$d3_scale$src$init");
  global = require("module$node_modules$d3_scale$src$nice");
  var default$$require$_DOT__SLASH_continuous_DOT_js = require.esmDefault(require$_DOT__SLASH_continuous_DOT_js), default$$require$_DOT__SLASH_nice_DOT_js = require.esmDefault(global);
};

//# sourceMappingURL=module$node_modules$d3_scale$src$time.js.map
