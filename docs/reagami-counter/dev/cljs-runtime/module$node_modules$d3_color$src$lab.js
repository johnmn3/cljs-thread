shadow$provide.module$node_modules$d3_color$src$lab = function(global, require, module, exports) {
  function labConvert(o) {
    if (o instanceof Lab) {
      return new Lab(o.l, o.a, o.b, o.opacity);
    }
    if (o instanceof Hcl) {
      return hcl2lab(o);
    }
    o instanceof require$_DOT__SLASH_color_DOT_js.Rgb || (o = (0,require$_DOT__SLASH_color_DOT_js.rgbConvert)(o));
    var r = rgb2lrgb(o.r), g = rgb2lrgb(o.g), b = rgb2lrgb(o.b), y = xyz2lab(0.2225045 * r + 0.7168786 * g + 0.0606169 * b);
    if (r === g && g === b) {
      var x = r = y;
    } else {
      x = xyz2lab((0.4360747 * r + 0.3850649 * g + 0.1430804 * b) / 0.96422), r = xyz2lab((0.0139322 * r + 0.0971045 * g + 0.7141733 * b) / 0.82521);
    }
    return new Lab(116 * y - 16, 500 * (x - y), 200 * (y - r), o.opacity);
  }
  function gray(l, opacity) {
    return new Lab(l, 0, 0, null == opacity ? 1 : opacity);
  }
  function lab(l, a, b, opacity) {
    return 1 === arguments.length ? labConvert(l) : new Lab(l, a, b, null == opacity ? 1 : opacity);
  }
  function Lab(l, a, b, opacity) {
    this.l = +l;
    this.a = +a;
    this.b = +b;
    this.opacity = +opacity;
  }
  function xyz2lab(t) {
    return t > t3 ? Math.pow(t, 1 / 3) : t / t2 + t0;
  }
  function lab2xyz(t) {
    return t > t1 ? t * t * t : t2 * (t - t0);
  }
  function lrgb2rgb(x) {
    return 255 * (0.0031308 >= x ? 12.92 * x : 1.055 * Math.pow(x, 1 / 2.4) - 0.055);
  }
  function rgb2lrgb(x) {
    return 0.04045 >= (x /= 255) ? x / 12.92 : Math.pow((x + 0.055) / 1.055, 2.4);
  }
  function hclConvert(o) {
    if (o instanceof Hcl) {
      return new Hcl(o.h, o.c, o.l, o.opacity);
    }
    o instanceof Lab || (o = labConvert(o));
    if (0 === o.a && 0 === o.b) {
      return new Hcl(NaN, 0 < o.l && 100 > o.l ? 0 : NaN, o.l, o.opacity);
    }
    var h = Math.atan2(o.b, o.a) * require$_DOT__SLASH_math_DOT_js.degrees;
    return new Hcl(0 > h ? h + 360 : h, Math.sqrt(o.a * o.a + o.b * o.b), o.l, o.opacity);
  }
  function lch(l, c, h, opacity) {
    return 1 === arguments.length ? hclConvert(l) : new Hcl(h, c, l, null == opacity ? 1 : opacity);
  }
  function hcl(h, c, l, opacity) {
    return 1 === arguments.length ? hclConvert(h) : new Hcl(h, c, l, null == opacity ? 1 : opacity);
  }
  function Hcl(h, c, l, opacity) {
    this.h = +h;
    this.c = +c;
    this.l = +l;
    this.opacity = +opacity;
  }
  function hcl2lab(o) {
    if (isNaN(o.h)) {
      return new Lab(o.l, 0, 0, o.opacity);
    }
    var h = o.h * require$_DOT__SLASH_math_DOT_js.radians;
    return new Lab(o.l, Math.cos(h) * o.c, Math.sin(h) * o.c, o.opacity);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Hcl:{enumerable:!0, get:function() {
    return Hcl;
  }}, Lab:{enumerable:!0, get:function() {
    return Lab;
  }}, default:{enumerable:!0, get:function() {
    return lab;
  }}, gray:{enumerable:!0, get:function() {
    return gray;
  }}, hcl:{enumerable:!0, get:function() {
    return hcl;
  }}, lch:{enumerable:!0, get:function() {
    return lch;
  }}});
  global = require("module$node_modules$d3_color$src$define");
  var require$_DOT__SLASH_color_DOT_js = require("module$node_modules$d3_color$src$color"), require$_DOT__SLASH_math_DOT_js = require("module$node_modules$d3_color$src$math");
  require = require.esmDefault(global);
  const t0 = 4 / 29, t1 = 6 / 29, t2 = 3 * t1 * t1, t3 = t1 * t1 * t1;
  (0,require.default)(Lab, lab, (0,global.extend)(require$_DOT__SLASH_color_DOT_js.Color, {brighter(k) {
    return new Lab(this.l + 18 * (null == k ? 1 : k), this.a, this.b, this.opacity);
  }, darker(k) {
    return new Lab(this.l - 18 * (null == k ? 1 : k), this.a, this.b, this.opacity);
  }, rgb() {
    var y = (this.l + 16) / 116, x = isNaN(this.a) ? y : y + this.a / 500, z = isNaN(this.b) ? y : y - this.b / 200;
    x = 0.96422 * lab2xyz(x);
    y = 1 * lab2xyz(y);
    z = 0.82521 * lab2xyz(z);
    return new require$_DOT__SLASH_color_DOT_js.Rgb(lrgb2rgb(3.1338561 * x - 1.6168667 * y - 0.4906146 * z), lrgb2rgb(-0.9787684 * x + 1.9161415 * y + 0.0334540 * z), lrgb2rgb(0.0719453 * x - 0.2289914 * y + 1.4052427 * z), this.opacity);
  }}));
  (0,require.default)(Hcl, hcl, (0,global.extend)(require$_DOT__SLASH_color_DOT_js.Color, {brighter(k) {
    return new Hcl(this.h, this.c, this.l + 18 * (null == k ? 1 : k), this.opacity);
  }, darker(k) {
    return new Hcl(this.h, this.c, this.l - 18 * (null == k ? 1 : k), this.opacity);
  }, rgb() {
    return hcl2lab(this).rgb();
  }}));
};

//# sourceMappingURL=module$node_modules$d3_color$src$lab.js.map
