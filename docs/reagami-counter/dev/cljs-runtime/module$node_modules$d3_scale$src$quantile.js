shadow$provide.module$node_modules$d3_scale$src$quantile = function(global, require, module, exports) {
  function quantile() {
    function rescale() {
      var i = 0, n = Math.max(1, range.length);
      for (thresholds = Array(n - 1); ++i < n;) {
        thresholds[i - 1] = (0,require$d3_array.quantileSorted)(domain, i / n);
      }
      return scale;
    }
    function scale(x) {
      return null == x || isNaN(x = +x) ? unknown : range[(0,require$d3_array.bisect)(thresholds, x)];
    }
    var domain = [], range = [], thresholds = [], unknown;
    scale.invertExtent = function(y) {
      y = range.indexOf(y);
      return 0 > y ? [NaN, NaN] : [0 < y ? thresholds[y - 1] : domain[0], y < thresholds.length ? thresholds[y] : domain[domain.length - 1]];
    };
    scale.domain = function(_) {
      if (!arguments.length) {
        return domain.slice();
      }
      domain = [];
      for (let d of _) {
        null == d || isNaN(d = +d) || domain.push(d);
      }
      domain.sort(require$d3_array.ascending);
      return rescale();
    };
    scale.range = function(_) {
      return arguments.length ? (range = Array.from(_), rescale()) : range.slice();
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    scale.quantiles = function() {
      return thresholds.slice();
    };
    scale.copy = function() {
      return quantile().domain(domain).range(range).unknown(unknown);
    };
    return require$_DOT__SLASH_init_DOT_js.initRange.apply(scale, arguments);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return quantile;
  }}});
  var require$d3_array = require("module$node_modules$d3_array$src$index"), require$_DOT__SLASH_init_DOT_js = require("module$node_modules$d3_scale$src$init");
};

//# sourceMappingURL=module$node_modules$d3_scale$src$quantile.js.map
