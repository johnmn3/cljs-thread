shadow$provide.module$node_modules$d3_interpolate$src$zoom = function(global, require, module, exports) {
  function cosh(x) {
    return ((x = Math.exp(x)) + 1 / x) / 2;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  const $$default = function zoomRho(rho, rho2, rho4) {
    function zoom(p0, p1) {
      var ux0 = p0[0], uy0 = p0[1], w0 = p0[2];
      p0 = p1[2];
      var dx = p1[0] - ux0, dy = p1[1] - uy0, d2 = dx * dx + dy * dy;
      if (1e-12 > d2) {
        var S = Math.log(p0 / w0) / rho;
        p0 = function(t) {
          return [ux0 + t * dx, uy0 + t * dy, w0 * Math.exp(rho * t * S)];
        };
      } else {
        var d1 = Math.sqrt(d2);
        p1 = (p0 * p0 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1);
        p0 = (p0 * p0 - w0 * w0 - rho4 * d2) / (2 * p0 * rho2 * d1);
        var r0 = Math.log(Math.sqrt(p1 * p1 + 1) - p1);
        S = (Math.log(Math.sqrt(p0 * p0 + 1) - p0) - r0) / rho;
        p0 = function(t) {
          t *= S;
          var coshr0 = cosh(r0), x = rho * t + r0;
          var JSCompiler_inline_result = ((x = Math.exp(2 * x)) - 1) / (x + 1);
          var x$jscomp$0 = r0;
          x = ((x$jscomp$0 = Math.exp(x$jscomp$0)) - 1 / x$jscomp$0) / 2;
          JSCompiler_inline_result = w0 / (rho2 * d1) * (coshr0 * JSCompiler_inline_result - x);
          return [ux0 + JSCompiler_inline_result * dx, uy0 + JSCompiler_inline_result * dy, w0 * coshr0 / cosh(rho * t + r0)];
        };
      }
      p0.duration = 1000 * S * rho / Math.SQRT2;
      return p0;
    }
    zoom.rho = function(_) {
      _ = Math.max(1e-3, +_);
      var _2 = _ * _;
      return zoomRho(_, _2, _2 * _2);
    };
    return zoom;
  }(Math.SQRT2, 2, 4);
};

//# sourceMappingURL=module$node_modules$d3_interpolate$src$zoom.js.map
