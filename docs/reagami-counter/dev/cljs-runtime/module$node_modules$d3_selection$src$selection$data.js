shadow$provide.module$node_modules$d3_selection$src$selection$data = function(global, require, module, exports) {
  function bindIndex(parent, group, enter, update, exit, data) {
    for (var i = 0, node, groupLength = group.length, dataLength = data.length; i < dataLength; ++i) {
      (node = group[i]) ? (node.__data__ = data[i], update[i] = node) : enter[i] = new require$_DOT__SLASH_enter_DOT_js.EnterNode(parent, data[i]);
    }
    for (; i < groupLength; ++i) {
      if (node = group[i]) {
        exit[i] = node;
      }
    }
  }
  function bindKey(parent, group, enter, update, exit, data, key) {
    var i, node, nodeByKeyValue = new Map(), groupLength = group.length, dataLength = data.length, keyValues = Array(groupLength), keyValue;
    for (i = 0; i < groupLength; ++i) {
      if (node = group[i]) {
        keyValues[i] = keyValue = key.call(node, node.__data__, i, group) + "", nodeByKeyValue.has(keyValue) ? exit[i] = node : nodeByKeyValue.set(keyValue, node);
      }
    }
    for (i = 0; i < dataLength; ++i) {
      keyValue = key.call(parent, data[i], i, data) + "", (node = nodeByKeyValue.get(keyValue)) ? (update[i] = node, node.__data__ = data[i], nodeByKeyValue.delete(keyValue)) : enter[i] = new require$_DOT__SLASH_enter_DOT_js.EnterNode(parent, data[i]);
    }
    for (i = 0; i < groupLength; ++i) {
      (node = group[i]) && nodeByKeyValue.get(keyValues[i]) === node && (exit[i] = node);
    }
  }
  function datum(node) {
    return node.__data__;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  var require$_DOT__SLASH_index_DOT_js = require("module$node_modules$d3_selection$src$selection$index"), require$_DOT__SLASH_enter_DOT_js = require("module$node_modules$d3_selection$src$selection$enter");
  global = require("module$node_modules$d3_selection$src$constant");
  var default$$require$_DOT__DOT__SLASH_constant_DOT_js = require.esmDefault(global);
  const $$default = function(value, key) {
    if (!arguments.length) {
      return Array.from(this, datum);
    }
    var bind = key ? bindKey : bindIndex, parents = this._parents, groups = this._groups;
    "function" !== typeof value && (value = (0,default$$require$_DOT__DOT__SLASH_constant_DOT_js.default)(value));
    for (var m = groups.length, update = Array(m), enter = Array(m), exit = Array(m), j = 0; j < m; ++j) {
      var parent = parents[j], group = groups[j], groupLength = group.length, data = value.call(parent, parent && parent.__data__, j, parents);
      var data$jscomp$0 = "object" === typeof data && "length" in data ? data : Array.from(data);
      data = data$jscomp$0.length;
      var enterGroup = enter[j] = Array(data), updateGroup = update[j] = Array(data);
      groupLength = exit[j] = Array(groupLength);
      bind(parent, group, enterGroup, updateGroup, groupLength, data$jscomp$0, key);
      for (group = parent = 0; parent < data; ++parent) {
        if (groupLength = enterGroup[parent]) {
          for (parent >= group && (group = parent + 1); !(data$jscomp$0 = updateGroup[group]) && ++group < data;) {
          }
          groupLength._next = data$jscomp$0 || null;
        }
      }
    }
    update = new require$_DOT__SLASH_index_DOT_js.Selection(update, parents);
    update._enter = enter;
    update._exit = exit;
    return update;
  };
};

//# sourceMappingURL=module$node_modules$d3_selection$src$selection$data.js.map
