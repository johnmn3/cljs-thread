shadow$provide.module$node_modules$d3_array$src$cross = function(global, require, module, exports) {
  function length(array) {
    return array.length | 0;
  }
  function empty(length) {
    return !(0 < length);
  }
  function arrayify(values) {
    return "object" !== typeof values || "length" in values ? values : Array.from(values);
  }
  function reducer(reduce) {
    return values => reduce(...values);
  }
  function cross(...values) {
    const reduce = "function" === typeof values[values.length - 1] && reducer(values.pop());
    values = values.map(arrayify);
    const lengths = values.map(length), j = values.length - 1, index = Array(j + 1).fill(0), product = [];
    if (0 > j || lengths.some(empty)) {
      return product;
    }
    for (;;) {
      product.push(index.map((j, i) => values[i][j]));
      let i = j;
      for (; ++index[i] === lengths[i];) {
        if (0 === i) {
          return reduce ? product.map(reduce) : product;
        }
        index[i--] = 0;
      }
    }
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return cross;
  }}});
};

//# sourceMappingURL=module$node_modules$d3_array$src$cross.js.map
