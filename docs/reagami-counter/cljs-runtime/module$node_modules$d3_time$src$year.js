shadow$provide.module$node_modules$d3_time$src$year = function(global, require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, timeYear:{enumerable:!0, get:function() {
    return timeYear;
  }}, timeYears:{enumerable:!0, get:function() {
    return timeYears;
  }}, utcYear:{enumerable:!0, get:function() {
    return utcYear;
  }}, utcYears:{enumerable:!0, get:function() {
    return utcYears;
  }}});
  var require$_DOT__SLASH_interval_DOT_js = require("module$node_modules$d3_time$src$interval");
  const timeYear = (0,require$_DOT__SLASH_interval_DOT_js.timeInterval)(date => {
    date.setMonth(0, 1);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setFullYear(date.getFullYear() + step);
  }, (start, end) => end.getFullYear() - start.getFullYear(), date => date.getFullYear());
  timeYear.every = k => isFinite(k = Math.floor(k)) && 0 < k ? (0,require$_DOT__SLASH_interval_DOT_js.timeInterval)(date => {
    date.setFullYear(Math.floor(date.getFullYear() / k) * k);
    date.setMonth(0, 1);
    date.setHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setFullYear(date.getFullYear() + step * k);
  }) : null;
  const timeYears = timeYear.range, utcYear = (0,require$_DOT__SLASH_interval_DOT_js.timeInterval)(date => {
    date.setUTCMonth(0, 1);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCFullYear(date.getUTCFullYear() + step);
  }, (start, end) => end.getUTCFullYear() - start.getUTCFullYear(), date => date.getUTCFullYear());
  utcYear.every = k => isFinite(k = Math.floor(k)) && 0 < k ? (0,require$_DOT__SLASH_interval_DOT_js.timeInterval)(date => {
    date.setUTCFullYear(Math.floor(date.getUTCFullYear() / k) * k);
    date.setUTCMonth(0, 1);
    date.setUTCHours(0, 0, 0, 0);
  }, (date, step) => {
    date.setUTCFullYear(date.getUTCFullYear() + step * k);
  }) : null;
  const utcYears = utcYear.range;
};

//# sourceMappingURL=module$node_modules$d3_time$src$year.js.map
