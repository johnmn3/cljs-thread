shadow$provide.module$node_modules$d3_geo$src$clip$rectangle = function(global, require, module, exports) {
  function clipRectangle(x0, y0, x1, y1) {
    function interpolate(from, to, direction, stream) {
      var a = 0, a1 = 0;
      if (null == from || (a = corner(from, direction)) !== (a1 = corner(to, direction)) || 0 > comparePoint(from, to) ^ 0 < direction) {
        do {
          stream.point(0 === a || 3 === a ? x0 : x1, 1 < a ? y1 : y0);
        } while ((a = (a + direction + 4) % 4) !== a1);
      } else {
        stream.point(to[0], to[1]);
      }
    }
    function corner(p, direction) {
      return (0,require$_DOT__DOT__SLASH_math_DOT_js.abs)(p[0] - x0) < require$_DOT__DOT__SLASH_math_DOT_js.epsilon ? 0 < direction ? 0 : 3 : (0,require$_DOT__DOT__SLASH_math_DOT_js.abs)(p[0] - x1) < require$_DOT__DOT__SLASH_math_DOT_js.epsilon ? 0 < direction ? 2 : 1 : (0,require$_DOT__DOT__SLASH_math_DOT_js.abs)(p[1] - y0) < require$_DOT__DOT__SLASH_math_DOT_js.epsilon ? 0 < direction ? 1 : 0 : 0 < direction ? 3 : 2;
    }
    function compareIntersection(a, b) {
      return comparePoint(a.x, b.x);
    }
    function comparePoint(a, b) {
      var ca = corner(a, 1), cb = corner(b, 1);
      return ca !== cb ? ca - cb : 0 === ca ? b[1] - a[1] : 1 === ca ? a[0] - b[0] : 2 === ca ? a[1] - b[1] : b[0] - a[0];
    }
    return function(stream) {
      function point(x, y) {
        x0 <= x && x <= x1 && y0 <= y && y <= y1 && activeStream.point(x, y);
      }
      function linePoint(x, y) {
        var v = x0 <= x && x <= x1 && y0 <= y && y <= y1;
        polygon && ring$jscomp$0.push([x, y]);
        if (first) {
          x__ = x, y__ = y, v__ = v, first = !1, v && (activeStream.lineStart(), activeStream.point(x, y));
        } else {
          if (v && v_) {
            activeStream.point(x, y);
          } else {
            var a = [x_ = Math.max(clipMin, Math.min(clipMax, x_)), y_ = Math.max(clipMin, Math.min(clipMax, y_))], b = [x = Math.max(clipMin, Math.min(clipMax, x)), y = Math.max(clipMin, Math.min(clipMax, y))];
            (0,default$$require$_DOT__SLASH_line_DOT_js.default)(a, b, x0, y0, x1, y1) ? (v_ || (activeStream.lineStart(), activeStream.point(a[0], a[1])), activeStream.point(b[0], b[1]), v || activeStream.lineEnd(), clean = !1) : v && (activeStream.lineStart(), activeStream.point(x, y), clean = !1);
          }
        }
        x_ = x;
        y_ = y;
        v_ = v;
      }
      var activeStream = stream, bufferStream = (0,default$$require$_DOT__SLASH_buffer_DOT_js.default)(), segments, polygon, ring$jscomp$0, x__, y__, v__, x_, y_, v_, first, clean, clipStream = {point, lineStart:function() {
        clipStream.point = linePoint;
        polygon && polygon.push(ring$jscomp$0 = []);
        first = !0;
        v_ = !1;
        x_ = y_ = NaN;
      }, lineEnd:function() {
        segments && (linePoint(x__, y__), v__ && v_ && bufferStream.rejoin(), segments.push(bufferStream.result()));
        clipStream.point = point;
        v_ && activeStream.lineEnd();
      }, polygonStart:function() {
        activeStream = bufferStream;
        segments = [];
        polygon = [];
        clean = !0;
      }, polygonEnd:function() {
        for (var startInside, i = startInside = 0, n = polygon.length; i < n; ++i) {
          var ring = polygon[i], j = 1, m = ring.length, point = ring[0], b0 = point[0];
          for (point = point[1]; j < m; ++j) {
            var a0 = b0;
            var a1 = point;
            point = ring[j];
            b0 = point[0];
            point = point[1];
            a1 <= y1 ? point > y1 && (b0 - a0) * (y1 - a1) > (point - a1) * (x0 - a0) && ++startInside : point <= y1 && (b0 - a0) * (y1 - a1) < (point - a1) * (x0 - a0) && --startInside;
          }
        }
        i = clean && startInside;
        n = (segments = (0,require$d3_array.merge)(segments)).length;
        if (i || n) {
          stream.polygonStart(), i && (stream.lineStart(), interpolate(null, null, 1, stream), stream.lineEnd()), n && (0,default$$require$_DOT__SLASH_rejoin_DOT_js.default)(segments, compareIntersection, startInside, interpolate, stream), stream.polygonEnd();
        }
        activeStream = stream;
        segments = polygon = ring$jscomp$0 = null;
      }};
      return clipStream;
    };
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return clipRectangle;
  }}});
  var require$_DOT__DOT__SLASH_math_DOT_js = require("module$node_modules$d3_geo$src$math");
  global = require("module$node_modules$d3_geo$src$clip$buffer");
  module = require("module$node_modules$d3_geo$src$clip$line");
  exports = require("module$node_modules$d3_geo$src$clip$rejoin");
  var require$d3_array = require("module$node_modules$d3_array$src$index"), default$$require$_DOT__SLASH_buffer_DOT_js = require.esmDefault(global), default$$require$_DOT__SLASH_rejoin_DOT_js = require.esmDefault(exports), default$$require$_DOT__SLASH_line_DOT_js = require.esmDefault(module), clipMax = 1e9, clipMin = -clipMax;
};

//# sourceMappingURL=module$node_modules$d3_geo$src$clip$rectangle.js.map
