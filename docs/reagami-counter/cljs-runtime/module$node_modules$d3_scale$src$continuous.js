shadow$provide.module$node_modules$d3_scale$src$continuous = function(global, require, module, exports) {
  function identity(x) {
    return x;
  }
  function normalize(a, b) {
    return (b -= a = +a) ? function(x) {
      return (x - a) / b;
    } : (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(isNaN(b) ? NaN : 0.5);
  }
  function clamper(a, b) {
    if (a > b) {
      var t = a;
      a = b;
      b = t;
    }
    return function(x) {
      return Math.max(a, Math.min(b, x));
    };
  }
  function bimap(domain, range, interpolate) {
    var d0 = domain[0];
    domain = domain[1];
    var r0 = range[0];
    range = range[1];
    domain < d0 ? (d0 = normalize(domain, d0), r0 = interpolate(range, r0)) : (d0 = normalize(d0, domain), r0 = interpolate(r0, range));
    return function(x) {
      return r0(d0(x));
    };
  }
  function polymap(domain, range, interpolate) {
    var j = Math.min(domain.length, range.length) - 1, d = Array(j), r = Array(j), i = -1;
    domain[j] < domain[0] && (domain = domain.slice().reverse(), range = range.slice().reverse());
    for (; ++i < j;) {
      d[i] = normalize(domain[i], domain[i + 1]), r[i] = interpolate(range[i], range[i + 1]);
    }
    return function(x) {
      var i = (0,require$d3_array.bisect)(domain, x, 1, j) - 1;
      return r[i](d[i](x));
    };
  }
  function copy(source, target) {
    return target.domain(source.domain()).range(source.range()).interpolate(source.interpolate()).clamp(source.clamp()).unknown(source.unknown());
  }
  function transformer() {
    function rescale() {
      var n = Math.min(domain.length, range.length);
      clamp !== identity && (clamp = clamper(domain[0], domain[n - 1]));
      piecewise = 2 < n ? polymap : bimap;
      output = input = null;
      return scale;
    }
    function scale(x) {
      return null == x || isNaN(x = +x) ? unknown : (output || (output = piecewise(domain.map(transform), range, interpolate)))(transform(clamp(x)));
    }
    var domain = unit, range = unit, interpolate = require$d3_interpolate.interpolate, transform, untransform, unknown, clamp = identity, piecewise, output, input;
    scale.invert = function(y) {
      return clamp(untransform((input || (input = piecewise(range, domain.map(transform), require$d3_interpolate.interpolateNumber)))(y)));
    };
    scale.domain = function(_) {
      return arguments.length ? (domain = Array.from(_, default$$require$_DOT__SLASH_number_DOT_js.default), rescale()) : domain.slice();
    };
    scale.range = function(_) {
      return arguments.length ? (range = Array.from(_), rescale()) : range.slice();
    };
    scale.rangeRound = function(_) {
      return range = Array.from(_), interpolate = require$d3_interpolate.interpolateRound, rescale();
    };
    scale.clamp = function(_) {
      return arguments.length ? (clamp = _ ? !0 : identity, rescale()) : clamp !== identity;
    };
    scale.interpolate = function(_) {
      return arguments.length ? (interpolate = _, rescale()) : interpolate;
    };
    scale.unknown = function(_) {
      return arguments.length ? (unknown = _, scale) : unknown;
    };
    return function(t, u) {
      transform = t;
      untransform = u;
      return rescale();
    };
  }
  function continuous() {
    return transformer()(identity, identity);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, copy:{enumerable:!0, get:function() {
    return copy;
  }}, default:{enumerable:!0, get:function() {
    return continuous;
  }}, identity:{enumerable:!0, get:function() {
    return identity;
  }}, transformer:{enumerable:!0, get:function() {
    return transformer;
  }}});
  var require$d3_array = require("module$node_modules$d3_array$src$index"), require$d3_interpolate = require("module$node_modules$d3_interpolate$src$index");
  global = require("module$node_modules$d3_scale$src$constant");
  module = require("module$node_modules$d3_scale$src$number");
  var default$$require$_DOT__SLASH_constant_DOT_js = require.esmDefault(global), default$$require$_DOT__SLASH_number_DOT_js = require.esmDefault(module), unit = [0, 1];
};

//# sourceMappingURL=module$node_modules$d3_scale$src$continuous.js.map
