shadow$provide.module$node_modules$canvas_confetti$src$confetti = function(global, require, module, exports) {
  (function main(global, module, isWorker, workerSize) {
    function noop() {
    }
    function promise(func) {
      var ModulePromise = module.exports.Promise;
      ModulePromise = void 0 !== ModulePromise ? ModulePromise : global.Promise;
      if ("function" === typeof ModulePromise) {
        return new ModulePromise(func);
      }
      func(noop, noop);
      return null;
    }
    function prop(options, name, transform) {
      var JSCompiler_temp;
      if (JSCompiler_temp = options) {
        JSCompiler_temp = options[name], JSCompiler_temp = !(null === JSCompiler_temp || void 0 === JSCompiler_temp);
      }
      options = JSCompiler_temp ? options[name] : defaults[name];
      return transform ? transform(options) : options;
    }
    function onlyPositiveInt(number) {
      return 0 > number ? 0 : Math.floor(number);
    }
    function colorsToRgb(colors) {
      return colors.map(hexToRgb);
    }
    function hexToRgb(str) {
      str = String(str).replace(/[^0-9a-f]/gi, "");
      6 > str.length && (str = str[0] + str[0] + str[1] + str[1] + str[2] + str[2]);
      return {r:parseInt(str.substring(0, 2), 16), g:parseInt(str.substring(2, 4), 16), b:parseInt(str.substring(4, 6), 16)};
    }
    function setCanvasWindowSize(canvas) {
      canvas.width = document.documentElement.clientWidth;
      canvas.height = document.documentElement.clientHeight;
    }
    function setCanvasRectSize(canvas) {
      var rect = canvas.getBoundingClientRect();
      canvas.width = rect.width;
      canvas.height = rect.height;
    }
    function getCanvas(zIndex) {
      var canvas = document.createElement("canvas");
      canvas.style.position = "fixed";
      canvas.style.top = "0px";
      canvas.style.left = "0px";
      canvas.style.pointerEvents = "none";
      canvas.style.zIndex = zIndex;
      return canvas;
    }
    function animate(canvas, fettis, resizer, size, done) {
      var animatingFettis = fettis.slice(), context = canvas.getContext("2d"), animationFrame, destroy, prom = promise(function(resolve) {
        function onDone() {
          animationFrame = destroy = null;
          context.clearRect(0, 0, size.width, size.height);
          bitmapMapper.clear();
          done();
          resolve();
        }
        function update() {
          !isWorker || size.width === workerSize.width && size.height === workerSize.height || (size.width = canvas.width = workerSize.width, size.height = canvas.height = workerSize.height);
          size.width || size.height || (resizer(canvas), size.width = canvas.width, size.height = canvas.height);
          context.clearRect(0, 0, size.width, size.height);
          animatingFettis = animatingFettis.filter(function(fetti) {
            fetti.x += Math.cos(fetti.angle2D) * fetti.velocity + fetti.drift;
            fetti.y += Math.sin(fetti.angle2D) * fetti.velocity + fetti.gravity;
            fetti.velocity *= fetti.decay;
            fetti.flat ? (fetti.wobble = 0, fetti.wobbleX = fetti.x + 10 * fetti.scalar, fetti.wobbleY = fetti.y + 10 * fetti.scalar, fetti.tiltSin = 0, fetti.tiltCos = 0, fetti.random = 1) : (fetti.wobble += fetti.wobbleSpeed, fetti.wobbleX = fetti.x + 10 * fetti.scalar * Math.cos(fetti.wobble), fetti.wobbleY = fetti.y + 10 * fetti.scalar * Math.sin(fetti.wobble), fetti.tiltAngle += 0.1, fetti.tiltSin = Math.sin(fetti.tiltAngle), fetti.tiltCos = Math.cos(fetti.tiltAngle), fetti.random = Math.random() + 
            2);
            var progress = fetti.tick++ / fetti.totalTicks, x1 = fetti.x + fetti.random * fetti.tiltCos, y1 = fetti.y + fetti.random * fetti.tiltSin, x2 = fetti.wobbleX + fetti.random * fetti.tiltCos, y2 = fetti.wobbleY + fetti.random * fetti.tiltSin;
            context.fillStyle = "rgba(" + fetti.color.r + ", " + fetti.color.g + ", " + fetti.color.b + ", " + (1 - progress) + ")";
            context.beginPath();
            if (canUsePaths && "path" === fetti.shape.type && "string" === typeof fetti.shape.path && Array.isArray(fetti.shape.matrix)) {
              progress = context.fill;
              var pathMatrix = fetti.shape.matrix, x = fetti.x, y = fetti.y;
              x1 = 0.1 * Math.abs(x2 - x1);
              y1 = 0.1 * Math.abs(y2 - y1);
              y2 = Math.PI / 10 * fetti.wobble;
              var path2d = new Path2D(fetti.shape.path);
              x2 = new Path2D();
              x2.addPath(path2d, new DOMMatrix(pathMatrix));
              pathMatrix = new Path2D();
              pathMatrix.addPath(x2, new DOMMatrix([Math.cos(y2) * x1, Math.sin(y2) * x1, -Math.sin(y2) * y1, Math.cos(y2) * y1, x, y]));
              progress.call(context, pathMatrix);
            } else if ("bitmap" === fetti.shape.type) {
              x = Math.PI / 10 * fetti.wobble, y = 0.1 * Math.abs(x2 - x1), pathMatrix = 0.1 * Math.abs(y2 - y1), y1 = fetti.shape.bitmap.width * fetti.scalar, y2 = fetti.shape.bitmap.height * fetti.scalar, x = new DOMMatrix([Math.cos(x) * y, Math.sin(x) * y, -Math.sin(x) * pathMatrix, Math.cos(x) * pathMatrix, fetti.x, fetti.y]), x.multiplySelf(new DOMMatrix(fetti.shape.matrix)), y = context.createPattern(bitmapMapper.transform(fetti.shape.bitmap), "no-repeat"), y.setTransform(x), context.globalAlpha = 
              1 - progress, context.fillStyle = y, context.fillRect(fetti.x - y1 / 2, fetti.y - y2 / 2, y1, y2), context.globalAlpha = 1;
            } else if ("circle" === fetti.shape) {
              context.ellipse ? context.ellipse(fetti.x, fetti.y, Math.abs(x2 - x1) * fetti.ovalScalar, Math.abs(y2 - y1) * fetti.ovalScalar, Math.PI / 10 * fetti.wobble, 0, 2 * Math.PI) : (progress = fetti.x, x = fetti.y, y = Math.abs(x2 - x1) * fetti.ovalScalar, y1 = Math.abs(y2 - y1) * fetti.ovalScalar, y2 = Math.PI / 10 * fetti.wobble, pathMatrix = 2 * Math.PI, context.save(), context.translate(progress, x), context.rotate(y2), context.scale(y, y1), context.arc(0, 0, 1, 0, pathMatrix, void 0), 
              context.restore());
            } else if ("star" === fetti.shape) {
              for (y1 = Math.PI / 2 * 3, y2 = 4 * fetti.scalar, progress = 8 * fetti.scalar, pathMatrix = 5, x1 = Math.PI / pathMatrix; pathMatrix--;) {
                x = fetti.x + Math.cos(y1) * progress, y = fetti.y + Math.sin(y1) * progress, context.lineTo(x, y), y1 += x1, x = fetti.x + Math.cos(y1) * y2, y = fetti.y + Math.sin(y1) * y2, context.lineTo(x, y), y1 += x1;
              }
            } else {
              context.moveTo(Math.floor(fetti.x), Math.floor(fetti.y)), context.lineTo(Math.floor(fetti.wobbleX), Math.floor(y1)), context.lineTo(Math.floor(x2), Math.floor(y2)), context.lineTo(Math.floor(x1), Math.floor(fetti.wobbleY));
            }
            context.closePath();
            context.fill();
            return fetti.tick < fetti.totalTicks;
          });
          animatingFettis.length ? animationFrame = raf.frame(update) : onDone();
        }
        animationFrame = raf.frame(update);
        destroy = onDone;
      });
      return {addFettis:function(fettis) {
        animatingFettis = animatingFettis.concat(fettis);
        return prom;
      }, canvas, promise:prom, reset:function() {
        animationFrame && raf.cancel(animationFrame);
        destroy && destroy();
      }};
    }
    function confettiCannon(canvas, globalOpts) {
      function fireLocal(options, size, done) {
        var particleCount = prop(options, "particleCount", onlyPositiveInt), angle = prop(options, "angle", Number), spread = prop(options, "spread", Number), startVelocity = prop(options, "startVelocity", Number), decay = prop(options, "decay", Number), gravity = prop(options, "gravity", Number), drift = prop(options, "drift", Number), colors = prop(options, "colors", colorsToRgb), ticks = prop(options, "ticks", Number), shapes = prop(options, "shapes"), scalar = prop(options, "scalar"), flat = 
        !!prop(options, "flat"), origin = prop(options, "origin", Object);
        origin.x = prop(origin, "x", Number);
        origin.y = prop(origin, "y", Number);
        options = [];
        var startX = canvas.width * origin.x;
        for (origin = canvas.height * origin.y; particleCount--;) {
          var JSCompiler_temp_const = options, JSCompiler_object_inline_startVelocity_506 = startVelocity, radSpread = Math.PI / 180 * spread;
          JSCompiler_temp_const.push.call(JSCompiler_temp_const, {x:startX, y:origin, wobble:10 * Math.random(), wobbleSpeed:Math.min(0.11, 0.1 * Math.random() + 0.05), velocity:0.5 * JSCompiler_object_inline_startVelocity_506 + Math.random() * JSCompiler_object_inline_startVelocity_506, angle2D:-(Math.PI / 180 * angle) + (0.5 * radSpread - Math.random() * radSpread), tiltAngle:(.5 * Math.random() + 0.25) * Math.PI, color:colors[particleCount % colors.length], shape:shapes[Math.floor(Math.random() * 
          (shapes.length - 0)) + 0], tick:0, totalTicks:ticks, decay, drift, random:Math.random() + 2, tiltSin:0, tiltCos:0, wobbleX:0, wobbleY:0, gravity:3 * gravity, ovalScalar:0.6, scalar, flat});
        }
        if (animationObj) {
          return animationObj.addFettis(options);
        }
        animationObj = animate(canvas, options, resizer, size, done);
        return animationObj.promise;
      }
      function fire(options) {
        function onResize() {
          if (worker) {
            var obj = {getBoundingClientRect:function() {
              if (!isLibCanvas) {
                return canvas.getBoundingClientRect();
              }
            }};
            resizer(obj);
            worker.postMessage({resize:{width:obj.width, height:obj.height}});
          } else {
            size.width = size.height = null;
          }
        }
        function done() {
          animationObj = null;
          allowResize && (hasResizeEventRegistered = !1, global.removeEventListener("resize", onResize));
          isLibCanvas && canvas && (document.body.contains(canvas) && document.body.removeChild(canvas), canvas = null, initialized = !1);
        }
        var disableForReducedMotion = globalDisableForReducedMotion || prop(options, "disableForReducedMotion", Boolean), zIndex = prop(options, "zIndex", Number);
        if (disableForReducedMotion && preferLessMotion) {
          return promise(function(resolve) {
            resolve();
          });
        }
        isLibCanvas && animationObj ? canvas = animationObj.canvas : isLibCanvas && !canvas && (canvas = getCanvas(zIndex), document.body.appendChild(canvas));
        allowResize && !initialized && resizer(canvas);
        var size = {width:canvas.width, height:canvas.height};
        worker && !initialized && worker.init(canvas);
        initialized = !0;
        worker && (canvas.__confetti_initialized = !0);
        allowResize && !hasResizeEventRegistered && (hasResizeEventRegistered = !0, global.addEventListener("resize", onResize, !1));
        return worker ? worker.fire(options, size, done) : fireLocal(options, size, done);
      }
      var isLibCanvas = !canvas, allowResize = !!prop(globalOpts || {}, "resize"), hasResizeEventRegistered = !1, globalDisableForReducedMotion = prop(globalOpts, "disableForReducedMotion", Boolean), worker = canUseWorker && prop(globalOpts || {}, "useWorker") ? getWorker() : null, resizer = isLibCanvas ? setCanvasWindowSize : setCanvasRectSize, initialized = canvas && worker ? !!canvas.__confetti_initialized : !1, preferLessMotion = "function" === typeof matchMedia && matchMedia("(prefers-reduced-motion)").matches, 
      animationObj;
      fire.reset = function() {
        worker && worker.reset();
        animationObj && animationObj.reset();
      };
      return fire;
    }
    function getDefaultFire() {
      defaultFire || (defaultFire = confettiCannon(null, {useWorker:!0, resize:!0}));
      return defaultFire;
    }
    var canUseWorker = !!(global.Worker && global.Blob && global.Promise && global.OffscreenCanvas && global.OffscreenCanvasRenderingContext2D && global.HTMLCanvasElement && global.HTMLCanvasElement.prototype.transferControlToOffscreen && global.URL && global.URL.createObjectURL), canUsePaths = "function" === typeof Path2D && "function" === typeof DOMMatrix, bitmapMapper = function(skipTransform, map) {
      return {transform:function(bitmap) {
        if (skipTransform) {
          return bitmap;
        }
        if (map.has(bitmap)) {
          return map.get(bitmap);
        }
        var canvas = new OffscreenCanvas(bitmap.width, bitmap.height);
        canvas.getContext("2d").drawImage(bitmap, 0, 0);
        map.set(bitmap, canvas);
        return canvas;
      }, clear:function() {
        map.clear();
      }};
    }(function() {
      if (!global.OffscreenCanvas) {
        return !1;
      }
      try {
        var canvas = new OffscreenCanvas(1, 1), ctx = canvas.getContext("2d");
        ctx.fillRect(0, 0, 1, 1);
        var bitmap = canvas.transferToImageBitmap();
        ctx.createPattern(bitmap, "no-repeat");
      } catch (e) {
        return !1;
      }
      return !0;
    }(), new Map()), raf = function() {
      var TIME = Math.floor(1000 / 60), frames = {}, lastFrameTime = 0;
      if ("function" === typeof requestAnimationFrame && "function" === typeof cancelAnimationFrame) {
        var frame = function(cb) {
          var id = Math.random();
          frames[id] = requestAnimationFrame(function onFrame(time) {
            lastFrameTime === time || lastFrameTime + TIME - 1 < time ? (lastFrameTime = time, delete frames[id], cb()) : frames[id] = requestAnimationFrame(onFrame);
          });
          return id;
        };
        var cancel = function(id) {
          frames[id] && cancelAnimationFrame(frames[id]);
        };
      } else {
        frame = function(cb) {
          return setTimeout(cb, TIME);
        }, cancel = function(timer) {
          return clearTimeout(timer);
        };
      }
      return {frame, cancel};
    }(), getWorker = function() {
      function decorate(worker) {
        worker.init = function(canvas) {
          canvas = canvas.transferControlToOffscreen();
          worker.postMessage({canvas}, [canvas]);
        };
        worker.fire = function(options, size, done) {
          if (prom) {
            return worker.postMessage({options:options || {}, callback:null}), prom;
          }
          var id = Math.random().toString(36).slice(2);
          return prom = promise(function(resolve) {
            function workerDone(msg) {
              msg.data.callback === id && (delete resolves[id], worker.removeEventListener("message", workerDone), prom = null, bitmapMapper.clear(), done(), resolve());
            }
            worker.addEventListener("message", workerDone);
            worker.postMessage({options:options || {}, callback:id});
            resolves[id] = workerDone.bind(null, {data:{callback:id}});
          });
        };
        worker.reset = function() {
          worker.postMessage({reset:!0});
          for (var id in resolves) {
            resolves[id](), delete resolves[id];
          }
        };
      }
      var worker, prom, resolves = {};
      return function() {
        if (worker) {
          return worker;
        }
        if (!isWorker && canUseWorker) {
          var code = ["var CONFETTI, SIZE \x3d {}, module \x3d {};", "(" + main.toString() + ")(this, module, true, SIZE);", "onmessage \x3d function(msg) {\n  if (msg.data.options) {\n    CONFETTI(msg.data.options).then(function () {\n      if (msg.data.callback) {\n        postMessage({ callback: msg.data.callback });\n      }\n    });\n  } else if (msg.data.reset) {\n    CONFETTI \x26\x26 CONFETTI.reset();\n  } else if (msg.data.resize) {\n    SIZE.width \x3d msg.data.resize.width;\n    SIZE.height \x3d msg.data.resize.height;\n  } else if (msg.data.canvas) {\n    SIZE.width \x3d msg.data.canvas.width;\n    SIZE.height \x3d msg.data.canvas.height;\n    CONFETTI \x3d module.exports.create(msg.data.canvas);\n  }\n}"].join("\n");
          try {
            worker = new Worker(URL.createObjectURL(new Blob([code])));
          } catch (e) {
            return "undefined" !== typeof console && "function" === typeof console.warn ? console.warn("\ud83c\udf8a Could not load worker", e) : null, null;
          }
          decorate(worker);
        }
        return worker;
      };
    }(), defaults = {particleCount:50, angle:90, spread:45, startVelocity:45, decay:0.9, gravity:1, drift:0, ticks:200, x:0.5, y:0.5, shapes:["square", "circle"], zIndex:100, colors:"#26ccff #a25afd #ff5e7e #88ff5a #fcff42 #ffa62d #ff36ff".split(" "), disableForReducedMotion:!1, scalar:1}, defaultFire;
    module.exports = function() {
      return getDefaultFire().apply(this, arguments);
    };
    module.exports.reset = function() {
      getDefaultFire().reset();
    };
    module.exports.create = confettiCannon;
    module.exports.shapeFromPath = function(pathData) {
      if (!canUsePaths) {
        throw Error("path confetti are not supported in this browser");
      }
      if ("string" === typeof pathData) {
        var path = pathData;
      } else {
        path = pathData.path;
        var matrix = pathData.matrix;
      }
      var path2d = new Path2D(path), tempCtx = document.createElement("canvas").getContext("2d");
      if (!matrix) {
        pathData = matrix = 1000;
        for (var maxX = 0, maxY = 0, x = 0; 1000 > x; x += 2) {
          for (var y = 0; 1000 > y; y += 2) {
            tempCtx.isPointInPath(path2d, x, y, "nonzero") && (matrix = Math.min(matrix, x), pathData = Math.min(pathData, y), maxX = Math.max(maxX, x), maxY = Math.max(maxY, y));
          }
        }
        path2d = maxX - matrix;
        tempCtx = maxY - pathData;
        maxY = Math.min(10 / path2d, 10 / tempCtx);
        matrix = [maxY, 0, 0, maxY, -Math.round(path2d / 2 + matrix) * maxY, -Math.round(tempCtx / 2 + pathData) * maxY];
      }
      return {type:"path", path, matrix};
    };
    module.exports.shapeFromText = function(textData) {
      var scalar = 1, color = "#000000", fontFamily = '"Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji", "EmojiOne Color", "Android Emoji", "Twemoji Mozilla", "system emoji", sans-serif';
      if ("string" === typeof textData) {
        var text = textData;
      } else {
        text = textData.text, scalar = "scalar" in textData ? textData.scalar : scalar, fontFamily = "fontFamily" in textData ? textData.fontFamily : fontFamily, color = "color" in textData ? textData.color : color;
      }
      textData = 10 * scalar;
      fontFamily = "" + textData + "px " + fontFamily;
      var canvas = new OffscreenCanvas(textData, textData), ctx = canvas.getContext("2d");
      ctx.font = fontFamily;
      canvas = ctx.measureText(text);
      textData = Math.ceil(canvas.actualBoundingBoxRight + canvas.actualBoundingBoxLeft);
      var height = Math.ceil(canvas.actualBoundingBoxAscent + canvas.actualBoundingBoxDescent), x = canvas.actualBoundingBoxLeft + 2, y = canvas.actualBoundingBoxAscent + 2;
      textData += 4;
      height += 4;
      canvas = new OffscreenCanvas(textData, height);
      ctx = canvas.getContext("2d");
      ctx.font = fontFamily;
      ctx.fillStyle = color;
      ctx.fillText(text, x, y);
      text = 1 / scalar;
      return {type:"bitmap", bitmap:canvas.transferToImageBitmap(), matrix:[text, 0, 0, text, -textData * text / 2, -height * text / 2]};
    };
  })(function() {
    return "undefined" !== typeof window ? window : "undefined" !== typeof self ? self : this || {};
  }(), module, !1);
};

//# sourceMappingURL=module$node_modules$canvas_confetti$src$confetti.js.map
