shadow$provide.module$node_modules$d3_selection$src$selection$on = function(global, require, module, exports) {
  function contextListener(listener) {
    return function(event) {
      listener.call(this, event, this.__data__);
    };
  }
  function parseTypenames(typenames) {
    return typenames.trim().split(/^|\s+/).map(function(t) {
      var name = "", i = t.indexOf(".");
      0 <= i && (name = t.slice(i + 1), t = t.slice(0, i));
      return {type:t, name};
    });
  }
  function onRemove(typename) {
    return function() {
      var on = this.__on;
      if (on) {
        for (var j = 0, i = -1, m = on.length, o; j < m; ++j) {
          (o = on[j], typename.type && o.type !== typename.type || o.name !== typename.name) ? on[++i] = o : this.removeEventListener(o.type, o.listener, o.options);
        }
        ++i ? on.length = i : delete this.__on;
      }
    };
  }
  function onAdd(typename, value, options) {
    return function() {
      var on = this.__on, o, listener = contextListener(value);
      if (on) {
        for (var j = 0, m = on.length; j < m; ++j) {
          if ((o = on[j]).type === typename.type && o.name === typename.name) {
            this.removeEventListener(o.type, o.listener, o.options);
            this.addEventListener(o.type, o.listener = listener, o.options = options);
            o.value = value;
            return;
          }
        }
      }
      this.addEventListener(typename.type, listener, options);
      o = {type:typename.type, name:typename.name, value, listener, options};
      on ? on.push(o) : this.__on = [o];
    };
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  const $$default = function(typename, value, options) {
    var typenames = parseTypenames(typename + ""), n = typenames.length, t;
    if (2 > arguments.length) {
      var on = this.node().__on;
      if (on) {
        for (var j = 0, m = on.length, o; j < m; ++j) {
          var i = 0;
          for (o = on[j]; i < n; ++i) {
            if ((t = typenames[i]).type === o.type && t.name === o.name) {
              return o.value;
            }
          }
        }
      }
    } else {
      on = value ? onAdd : onRemove;
      for (i = 0; i < n; ++i) {
        this.each(on(typenames[i], value, options));
      }
      return this;
    }
  };
};

//# sourceMappingURL=module$node_modules$d3_selection$src$selection$on.js.map
