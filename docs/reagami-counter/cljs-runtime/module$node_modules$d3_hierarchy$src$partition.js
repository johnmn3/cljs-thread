shadow$provide.module$node_modules$d3_hierarchy$src$partition = function(global, require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  global = require("module$node_modules$d3_hierarchy$src$treemap$round");
  module = require("module$node_modules$d3_hierarchy$src$treemap$dice");
  var default$$require$_DOT__SLASH_treemap_SLASH_dice_DOT_js = require.esmDefault(module), default$$require$_DOT__SLASH_treemap_SLASH_round_DOT_js = require.esmDefault(global);
  const $$default = function() {
    function partition(root) {
      var n = root.height + 1;
      root.x0 = root.y0 = padding;
      root.x1 = dx;
      root.y1 = dy / n;
      root.eachBefore(positionNode(dy, n));
      round && root.eachBefore(default$$require$_DOT__SLASH_treemap_SLASH_round_DOT_js.default);
      return root;
    }
    function positionNode(dy, n) {
      return function(node) {
        node.children && (0,default$$require$_DOT__SLASH_treemap_SLASH_dice_DOT_js.default)(node, node.x0, dy * (node.depth + 1) / n, node.x1, dy * (node.depth + 2) / n);
        var x0 = node.x0, y0 = node.y0, x1 = node.x1 - padding, y1 = node.y1 - padding;
        x1 < x0 && (x0 = x1 = (x0 + x1) / 2);
        y1 < y0 && (y0 = y1 = (y0 + y1) / 2);
        node.x0 = x0;
        node.y0 = y0;
        node.x1 = x1;
        node.y1 = y1;
      };
    }
    var dx = 1, dy = 1, padding = 0, round = !1;
    partition.round = function(x) {
      return arguments.length ? (round = !!x, partition) : round;
    };
    partition.size = function(x) {
      return arguments.length ? (dx = +x[0], dy = +x[1], partition) : [dx, dy];
    };
    partition.padding = function(x) {
      return arguments.length ? (padding = +x, partition) : padding;
    };
    return partition;
  };
};

//# sourceMappingURL=module$node_modules$d3_hierarchy$src$partition.js.map
