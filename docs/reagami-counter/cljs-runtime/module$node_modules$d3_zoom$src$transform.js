shadow$provide.module$node_modules$d3_zoom$src$transform = function(global, require, module, exports) {
  function Transform(k, x, y) {
    this.k = k;
    this.x = x;
    this.y = y;
  }
  function transform(node) {
    for (; !node.__zoom;) {
      if (!(node = node.parentNode)) {
        return identity;
      }
    }
    return node.__zoom;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Transform:{enumerable:!0, get:function() {
    return Transform;
  }}, default:{enumerable:!0, get:function() {
    return transform;
  }}, identity:{enumerable:!0, get:function() {
    return identity;
  }}});
  Transform.prototype = {constructor:Transform, scale:function(k) {
    return 1 === k ? this : new Transform(this.k * k, this.x, this.y);
  }, translate:function(x, y) {
    return 0 === x & 0 === y ? this : new Transform(this.k, this.x + this.k * x, this.y + this.k * y);
  }, apply:function(point) {
    return [point[0] * this.k + this.x, point[1] * this.k + this.y];
  }, applyX:function(x) {
    return x * this.k + this.x;
  }, applyY:function(y) {
    return y * this.k + this.y;
  }, invert:function(location) {
    return [(location[0] - this.x) / this.k, (location[1] - this.y) / this.k];
  }, invertX:function(x) {
    return (x - this.x) / this.k;
  }, invertY:function(y) {
    return (y - this.y) / this.k;
  }, rescaleX:function(x) {
    return x.copy().domain(x.range().map(this.invertX, this).map(x.invert, x));
  }, rescaleY:function(y) {
    return y.copy().domain(y.range().map(this.invertY, this).map(y.invert, y));
  }, toString:function() {
    return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
  }};
  var identity = new Transform(1, 0, 0);
  transform.prototype = Transform.prototype;
};

//# sourceMappingURL=module$node_modules$d3_zoom$src$transform.js.map
