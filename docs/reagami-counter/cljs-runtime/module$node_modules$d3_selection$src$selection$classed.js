shadow$provide.module$node_modules$d3_selection$src$selection$classed = function(global, require, module, exports) {
  function classList(node) {
    return node.classList || new ClassList(node);
  }
  function ClassList(node) {
    this._node = node;
    this._names = (node.getAttribute("class") || "").trim().split(/^|\s+/);
  }
  function classedAdd(node, names) {
    node = classList(node);
    for (var i = -1, n = names.length; ++i < n;) {
      node.add(names[i]);
    }
  }
  function classedRemove(node, names) {
    node = classList(node);
    for (var i = -1, n = names.length; ++i < n;) {
      node.remove(names[i]);
    }
  }
  function classedTrue(names) {
    return function() {
      classedAdd(this, names);
    };
  }
  function classedFalse(names) {
    return function() {
      classedRemove(this, names);
    };
  }
  function classedFunction(names, value) {
    return function() {
      (value.apply(this, arguments) ? classedAdd : classedRemove)(this, names);
    };
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  ClassList.prototype = {add:function(name) {
    0 > this._names.indexOf(name) && (this._names.push(name), this._node.setAttribute("class", this._names.join(" ")));
  }, remove:function(name) {
    name = this._names.indexOf(name);
    0 <= name && (this._names.splice(name, 1), this._node.setAttribute("class", this._names.join(" ")));
  }, contains:function(name) {
    return 0 <= this._names.indexOf(name);
  }};
  const $$default = function(name, value) {
    var names = (name + "").trim().split(/^|\s+/);
    if (2 > arguments.length) {
      for (var list = classList(this.node()), i = -1, n = names.length; ++i < n;) {
        if (!list.contains(names[i])) {
          return !1;
        }
      }
      return !0;
    }
    return this.each(("function" === typeof value ? classedFunction : value ? classedTrue : classedFalse)(names, value));
  };
};

//# sourceMappingURL=module$node_modules$d3_selection$src$selection$classed.js.map
